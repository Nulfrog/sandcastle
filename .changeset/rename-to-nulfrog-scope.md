---
"@nulfrog/sandcastle": minor
---

Rename the package from `@ai-hero/sandcastle` to `@nulfrog/sandcastle` and point `repository.url` at the `Nulfrog/sandcastle` fork. Update all imports across the source, scaffolded templates, and docs to the new name. This is a breaking change: consumers must update their import specifiers and install source.
