---
"@ai-hero/sandcastle": patch
---

Update README install instructions to target the `Nulfrog/sandcastle` fork (install from GitHub instead of the npm registry), add a pnpm install example, and document the `nulfrog-castle` init template.
